# Test Environment Configuration

# Disable Convex for tests (tests don't need backend)
VITE_USE_CONVEX=false
VITE_CONVEX_URL=

# Legacy API URL (not used in tests, but required by config)
VITE_API_URL=http://localhost:3001/api
