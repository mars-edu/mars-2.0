# MARS MCP — AI Tools Integration Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Give the MARS AI chat assistant access to live Convex data via AI SDK tools, so it can answer questions about grades, journals, schedule, students, teachers, and more instead of saying "go to the menu."

**Architecture:** JWT from `userStore.token` is sent in `Authorization` header → Convex HTTP action validates it, resolves user + role (including linked teacher/student IDs), builds a role-scoped tool set via `createMarsTools(ctx, user)` → `streamText` with `maxSteps: 5` enables multi-step tool calling. Unauthenticated requests receive no tools but still get general navigation help (no 401 — better UX than blocking chat entirely; deviation from spec intentional).

**Tech Stack:** Convex HTTP actions, `@ai-sdk/google` + Gemini 2.0 Flash, Vercel AI SDK `tool()` + `streamText`, Zod for parameter schemas, `jose` JWT validation, Vue 3 + Pinia `userStore`.

**Spec:** `docs/superpowers/specs/2026-03-11-mars-mcp-design.md`

**Task order is strict:** Task 1 → Task 2 → Task 3 → Task 4 → Task 5. Each task depends on the previous.

---

## Chunk 1: Backend — Helper Queries + Tools + Auth

### Task 1: Add helper queries for student data access

**Context:** The `students` table has no `userId` field, and `journalStudents` has no Convex query to look up journals by studentId. These two helper queries are needed before `tools.ts` can serve student users.

**Files:**
- Modify: `convex/journals/queries.ts`
- Modify: `convex/marks/queries.ts`

- [ ] **Step 1: Add `getByStudentId` to `convex/journals/queries.ts`**

Append to the end of `convex/journals/queries.ts`:

```typescript
export const getByStudentId = query({
  args: { studentId: v.string() },
  handler: async (ctx, args) => {
    const rows = await ctx.db
      .query('journalStudents')
      .withIndex('by_student', (q) => q.eq('studentId', args.studentId))
      .collect();
    const journals = await Promise.all(
      rows.map((r) => ctx.db.get(r.journalId)),
    );
    return journals.filter(Boolean);
  },
});
```

- [ ] **Step 2: Add `getAllByStudentId` to `convex/marks/queries.ts`**

Append to the end of `convex/marks/queries.ts`:

```typescript
export const getAllByStudentId = query({
  args: { studentId: v.string() },
  handler: async (ctx, args) => {
    return ctx.db
      .query('marks')
      .withIndex('by_student', (q) => q.eq('studentId', args.studentId))
      .collect();
  },
});
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npx convex dev --once 2>&1 | tail -5
```

Expected: `✔ Convex functions ready!`

- [ ] **Step 4: Commit**

```bash
git add convex/journals/queries.ts convex/marks/queries.ts
git commit -m "feat(ai): add getByStudentId and getAllByStudentId helper queries"
```

---

### Task 2: Create `convex/livekit/tools.ts`

**Depends on Task 1** (uses `api.journals.queries.getByStudentId` and `api.marks.queries.getAllByStudentId`).

**Context:**
- `journalStudents` rows: `{ journalId, studentId: string, createdAt }` — no name fields
- `calendarEvents.participants` is `string[]` of student IDs
- `getTeacherSubstitutions` takes `{ toUserId: v.id("users") }` — NOT teacherId
- `getStudentMarks` takes `{ journalId, studentId: string }`
- `students.queries.search` arg is `{ searchTerm: v.string() }` (not `query`)

**Files:**
- Create: `convex/livekit/tools.ts`

- [ ] **Step 1: Create the file**

```typescript
// convex/livekit/tools.ts
import { tool } from 'ai';
import { z } from 'zod';
import { api } from '../_generated/api';
import type { ActionCtx } from '../_generated/server';
import type { Id } from '../_generated/dataModel';

export interface ResolvedUser {
  userId: Id<'users'>;
  roles: string[];
  firstName: string;
  lastName: string;
  teacherId?: Id<'teachers'>;
  studentId?: string; // students._id as string (no userId index on students table)
}

export function createMarsTools(ctx: ActionCtx, user: ResolvedUser) {
  const isAdmin = user.roles.includes('ADMIN');
  const isTeacher = user.roles.includes('TEACHER');
  const isStudent = user.roles.includes('STUDENT');

  return {
    // ── Identity ─────────────────────────────────────────────────────────────
    getCurrentUser: tool({
      description: 'Get the current logged-in user: name, roles, linked IDs.',
      parameters: z.object({}),
      execute: async () => ({
        name: `${user.firstName} ${user.lastName}`,
        roles: user.roles,
        teacherId: user.teacherId ?? null,
        studentId: user.studentId ?? null,
      }),
    }),

    // ── Academic Year ─────────────────────────────────────────────────────────
    getAcademicYear: tool({
      description: 'Get the current active academic year and its semesters.',
      parameters: z.object({}),
      execute: async () => {
        const year = await ctx.runQuery(api.academicYears.queries.getActive, {});
        return year ?? { error: 'No active academic year found' };
      },
    }),

    // ── Disciplines ───────────────────────────────────────────────────────────
    getDisciplineList: tool({
      description: 'List all disciplines (subjects) in the system.',
      parameters: z.object({}),
      execute: async () => ctx.runQuery(api.disciplines.queries.list, {}),
    }),

    // ── Journals ──────────────────────────────────────────────────────────────
    listMyJournals: tool({
      description:
        'List journals for the current user. Teachers see their own journals. Students see journals they are enrolled in. Admins see all.',
      parameters: z.object({}),
      execute: async () => {
        if (isAdmin) {
          return ctx.runQuery(api.journals.queries.list, {});
        }
        if (isTeacher && user.teacherId) {
          const events = await ctx.runQuery(
            api.calendarEvents.queries.getByTeacher,
            { teacherId: user.teacherId },
          );
          const eventIds = new Set((events as any[]).map((e: any) => e._id));
          const allJournals = await ctx.runQuery(api.journals.queries.list, {});
          return (allJournals as any[]).filter((j: any) =>
            eventIds.has(j.calendarEventId),
          );
        }
        if (isStudent && user.studentId) {
          return ctx.runQuery(api.journals.queries.getByStudentId, {
            studentId: user.studentId,
          });
        }
        return { error: 'Access denied' };
      },
    }),

    getJournalMarks: tool({
      description:
        'Get marks in a specific journal. Teachers and admins see all marks. Students only see their own marks.',
      parameters: z.object({
        journalId: z.string().describe('The journal _id'),
      }),
      execute: async ({ journalId }) => {
        if (isAdmin || isTeacher) {
          return ctx.runQuery(api.marks.queries.getJournalMarks, {
            journalId: journalId as Id<'journals'>,
          });
        }
        if (isStudent && user.studentId) {
          return ctx.runQuery(api.marks.queries.getStudentMarks, {
            journalId: journalId as Id<'journals'>,
            studentId: user.studentId,
          });
        }
        return { error: 'Access denied' };
      },
    }),

    getMyMarks: tool({
      description:
        'Get all marks for the current student across all enrolled journals. Only works for STUDENT role.',
      parameters: z.object({}),
      execute: async () => {
        if (!isStudent || !user.studentId) {
          return { error: 'Only students can use this tool' };
        }
        return ctx.runQuery(api.marks.queries.getAllByStudentId, {
          studentId: user.studentId,
        });
      },
    }),

    // ── Schedule ──────────────────────────────────────────────────────────────
    getSchedule: tool({
      description:
        'Get calendar events / schedule. Teachers see their own events. Students see events they are participants in. Admins see all. Optionally filter by date range (ISO date strings).',
      parameters: z.object({
        startDate: z.string().optional().describe('ISO date, e.g. 2026-03-01'),
        endDate: z.string().optional().describe('ISO date, e.g. 2026-03-31'),
      }),
      execute: async ({ startDate, endDate }) => {
        if (isTeacher && user.teacherId) {
          const events = await ctx.runQuery(
            api.calendarEvents.queries.getByTeacher,
            { teacherId: user.teacherId },
          );
          return applyDateFilter(events as any[], startDate, endDate);
        }
        if (isAdmin) {
          if (startDate && endDate) {
            return ctx.runQuery(api.calendarEvents.queries.getByDateRange, {
              startDate,
              endDate,
            });
          }
          return ctx.runQuery(api.calendarEvents.queries.list, {});
        }
        if (isStudent && user.studentId) {
          const allEvents = await ctx.runQuery(
            api.calendarEvents.queries.list,
            {},
          );
          const myEvents = (allEvents as any[]).filter((e: any) =>
            Array.isArray(e.participants) &&
            e.participants.includes(user.studentId),
          );
          return applyDateFilter(myEvents, startDate, endDate);
        }
        return { error: 'Access denied' };
      },
    }),

    // ── Students ──────────────────────────────────────────────────────────────
    getStudentList: tool({
      description:
        'List students. Optionally filter by specialtyId or search by name. Teacher and admin only.',
      parameters: z.object({
        search: z.string().optional().describe('Name fragment to search'),
        specialtyId: z.string().optional().describe('Filter by specialty ID'),
      }),
      execute: async ({ search, specialtyId }) => {
        if (!isAdmin && !isTeacher) return { error: 'Access denied' };
        if (search) {
          return ctx.runQuery(api.students.queries.search, {
            searchTerm: search,
          });
        }
        if (specialtyId) {
          return ctx.runQuery(api.students.queries.getBySpecialty, {
            specialtyId: specialtyId as Id<'specialties'>,
          });
        }
        return ctx.runQuery(api.students.queries.list, {});
      },
    }),

    getStudentCard: tool({
      description:
        'Get full info for one student by their ID. Teacher and admin only.',
      parameters: z.object({
        studentId: z.string().describe('The student _id'),
      }),
      execute: async ({ studentId }) => {
        if (!isAdmin && !isTeacher) return { error: 'Access denied' };
        const student = await ctx.runQuery(api.students.queries.getById, {
          id: studentId as Id<'students'>,
        });
        return student ?? { error: 'Student not found' };
      },
    }),

    // ── Teachers ──────────────────────────────────────────────────────────────
    getTeacherList: tool({
      description: 'List all teachers. Admin only.',
      parameters: z.object({
        search: z.string().optional().describe('Name fragment to search'),
      }),
      execute: async ({ search }) => {
        if (!isAdmin) return { error: 'Access denied — admin only' };
        if (search) {
          return ctx.runQuery(api.teachers.queries.search, { query: search });
        }
        return ctx.runQuery(api.teachers.queries.list, {});
      },
    }),

    // ── KTP ───────────────────────────────────────────────────────────────────
    getKTP: tool({
      description:
        'Get the calendar-thematic plan (КТП) for a calendar event. Teacher and admin only.',
      parameters: z.object({
        eventId: z.string().describe('calendarEvent _id'),
      }),
      execute: async ({ eventId }) => {
        if (!isAdmin && !isTeacher) return { error: 'Access denied' };
        return ctx.runQuery(api.ktps.queries.getByEventId, {
          eventId: eventId as Id<'calendarEvents'>,
        });
      },
    }),

    // ── Substitutions ─────────────────────────────────────────────────────────
    getSubstitutions: tool({
      description:
        'Get substitution (замена) requests involving the current teacher. Optionally filter by status.',
      parameters: z.object({
        status: z
          .enum(['pending', 'accepted', 'rejected', 'completed'])
          .optional()
          .describe('Filter by status'),
      }),
      execute: async ({ status }) => {
        // getTeacherSubstitutions uses toUserId (the user receiving the substitution)
        if (isTeacher) {
          return ctx.runQuery(
            api.substitutions.queries.getTeacherSubstitutions,
            { toUserId: user.userId, status },
          );
        }
        if (isAdmin && user.teacherId) {
          return ctx.runQuery(
            api.substitutions.queries.getCreatedSubstitutions,
            { fromTeacherId: user.teacherId, status },
          );
        }
        // Admin without teacherId: no all-substitutions query exists yet
        return { error: 'No substitution query available for this role. Use the Substitutions page instead.' };
      },
    }),

    // ── Notifications ─────────────────────────────────────────────────────────
    getNotifications: tool({
      description: 'Get unread notifications for the current user.',
      parameters: z.object({}),
      execute: async () =>
        ctx.runQuery(api.notifications.queries.getUserNotifications, {
          userId: user.userId,
          status: 'unread',
        }),
    }),
  };
}

// ── Helpers ───────────────────────────────────────────────────────────────────
function applyDateFilter(
  events: any[],
  startDate?: string,
  endDate?: string,
): any[] {
  return events.filter((e) => {
    if (startDate && e.startDate < startDate) return false;
    if (endDate && e.endDate > endDate) return false;
    return true;
  });
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx convex dev --once 2>&1 | tail -5
```

Expected: `✔ Convex functions ready!` — fix any errors before continuing.

- [ ] **Step 3: Commit**

```bash
git add convex/livekit/tools.ts
git commit -m "feat(ai): add createMarsTools with 13 role-scoped tools"
```

---

### Task 3: Update system prompt builder

**Depends on Task 2** (Task 4's `chat.ts` imports `buildSystemPrompt` from this file).

**Files:**
- Modify: `convex/livekit/marsSystemPrompt.ts`

- [ ] **Step 1: Replace entire file content**

```typescript
// convex/livekit/marsSystemPrompt.ts

const MARS_BASE_PROMPT = `Ты — ИИ-ассистент системы MARS 2.0 (Минимальная Автоматизация Расписания Специальностей). Это система управления образованием для казахстанских колледжей.

## Твоя роль
Помогай пользователям (администраторам, преподавателям, студентам) разобраться в системе, находить нужные функции и решать задачи в текстовом чате.

## Структура системы MARS 2.0

### Роли: ADMIN, TEACHER, STUDENT, PARENT

### Модули:
- **Учебная структура**: academicYears, semesterDefinitions, specialties, courses, bases
- **Учебные планы**: disciplines, class9Items (модули с часами: лекции/лаб/практика/СРСП/СРС/учпрактика), rupEntries (РУП — страница /rup)
- **КТП**: ktps + ktpDetails — календарно-тематическое планирование (страница /ktp)
- **Расписание**: calendarEvents, educationSchedules (страница /planning)
- **Журналы**: journals, marks, markHistory, journalStudents (страницы /journals, /journal-details)
- **Контроль**: intermediateControls, finalControls, scheduled* версии
- **Люди**: students, teachers (страницы /student-card, /teacher-card)
- **Замены**: substitutions — workflow передачи журнала (pending→accepted→completed)
- **Отчёты**: /protocol, /analytics, /reports
- **Уведомления**: /notifications

### Навигация (боковое меню):
Главная → Каталог специальностей → Каталог дисциплин → Расписание → Протокол → Журналы → РУП → Аналитика → Отчёты → Расписание занятий → Карточка студента → Карточка преподавателя

## Стиль
- Краткие, конкретные ответы
- Русский язык по умолчанию, казахский если пользователь пишет на казахском
- При вопросе "где найти X" — указывай конкретный раздел меню или URL
- Markdown для форматирования допускается
`;

const TOOLS_INSTRUCTION = `
## Инструменты (реальные данные)
У тебя есть доступ к реальным данным системы через инструменты.
**ВСЕГДА** используй инструменты для ответа на вопросы о данных пользователя.
**НЕ говори** "перейдите в раздел" — получи данные сам через инструменты и покажи их.

Примеры когда использовать инструменты:
- "Какие у меня оценки?" → вызови getMyMarks или getJournalMarks
- "Покажи моё расписание" → вызови getSchedule
- "Сколько студентов в группе?" → вызови getStudentList
- "Есть ли уведомления?" → вызови getNotifications
- "Какие журналы у меня?" → вызови listMyJournals
`;

export function buildSystemPrompt(user: {
  firstName: string;
  lastName: string;
  roles: string[];
  teacherId?: string | null;
  studentId?: string | null;
} | null): string {
  if (!user) {
    return MARS_BASE_PROMPT;
  }

  const idLine = user.teacherId
    ? `ID преподавателя: ${user.teacherId}`
    : user.studentId
    ? `ID студента: ${user.studentId}`
    : '';

  const userContext = `
## Текущий пользователь
Имя: ${user.firstName} ${user.lastName}
Роль: ${user.roles.join(', ')}
${idLine}
`;

  return MARS_BASE_PROMPT + userContext + TOOLS_INSTRUCTION;
}
```

- [ ] **Step 2: Verify TypeScript compiles**

```bash
npx convex dev --once 2>&1 | tail -5
```

Expected: `✔ Convex functions ready!`

- [ ] **Step 3: Commit**

```bash
git add convex/livekit/marsSystemPrompt.ts
git commit -m "feat(ai): make system prompt dynamic with user context and tool instructions"
```

---

### Task 4: Refactor `chat.ts` — JWT auth + wire tools

**Depends on Tasks 2 and 3** (imports `createMarsTools` and `buildSystemPrompt`).

**Files:**
- Modify: `convex/livekit/chat.ts`
- Modify: `convex/http.ts`

**Context:**
- `validateToken(token, secret)` → `{ userId: string; roles: string[] }` from `convex/auth/helpers.ts`
- JWT secret env var: `process.env.JWT_SECRET`
- `api.users.queries.getUserProfile` takes `{ userId: v.id('users') }` — returns `{ firstName, lastName, ... }`
- `api.teachers.queries.getByUserId` takes `{ userId: v.id('users') }`
- `api.students.queries.search` takes `{ searchTerm: v.string() }` — returns `{ _id, firstName, surname, ... }[]`
- Students have no `userId` field — resolved by name match
- The HTTP action in `convex/http.ts` must pass `ctx` to `handleChatRequest`

- [ ] **Step 1: Replace entire `convex/livekit/chat.ts`**

```typescript
// convex/livekit/chat.ts
import { createGoogleGenerativeAI } from '@ai-sdk/google';
import { streamText, convertToModelMessages, type UIMessage } from 'ai';
import type { ActionCtx } from '../_generated/server';
import { api } from '../_generated/api';
import { validateToken } from '../auth/helpers';
import { buildSystemPrompt } from './marsSystemPrompt';
import { createMarsTools, type ResolvedUser } from './tools';
import type { Id } from '../_generated/dataModel';

const CORS_HEADERS = {
  'Access-Control-Allow-Origin': '*',
  'Access-Control-Allow-Methods': 'POST, OPTIONS',
  'Access-Control-Allow-Headers': '*',
};

function errorResponse(message: string, status: number): Response {
  return new Response(JSON.stringify({ error: message }), {
    status,
    headers: { 'Content-Type': 'application/json', ...CORS_HEADERS },
  });
}

async function resolveUser(
  ctx: ActionCtx,
  token: string,
): Promise<ResolvedUser | null> {
  const jwtSecret = process.env.JWT_SECRET;
  if (!jwtSecret) return null;

  let payload: { userId: string; roles: string[] };
  try {
    payload = await validateToken(token, jwtSecret);
  } catch {
    return null;
  }

  const userId = payload.userId as Id<'users'>;
  const profile = await ctx.runQuery(api.users.queries.getUserProfile, {
    userId,
  });
  if (!profile) return null;

  let teacherId: Id<'teachers'> | undefined;
  let studentId: string | undefined;

  if (payload.roles.includes('TEACHER')) {
    const teacher = await ctx.runQuery(api.teachers.queries.getByUserId, {
      userId,
    });
    teacherId = teacher?._id;
  }

  if (payload.roles.includes('STUDENT')) {
    // Students table has no userId — resolve by name match
    const matches = await ctx.runQuery(api.students.queries.search, {
      searchTerm: profile.firstName,
    });
    const match = (matches as any[]).find(
      (s: any) =>
        s.firstName === profile.firstName && s.surname === profile.lastName,
    );
    studentId = match?._id;
  }

  return {
    userId,
    roles: payload.roles,
    firstName: profile.firstName,
    lastName: profile.lastName,
    teacherId,
    studentId,
  };
}

export async function handleChatRequest(
  ctx: ActionCtx,
  request: Request,
): Promise<Response> {
  const googleApiKey = process.env.GOOGLE_API_KEY;
  if (!googleApiKey) {
    return errorResponse('GOOGLE_API_KEY not configured', 500);
  }

  // ── Auth ──────────────────────────────────────────────────────────────────
  const authHeader = request.headers.get('Authorization') ?? '';
  const token = authHeader.startsWith('Bearer ') ? authHeader.slice(7) : '';
  const user = token ? await resolveUser(ctx, token) : null;

  // ── Body ──────────────────────────────────────────────────────────────────
  let messages: UIMessage[];
  try {
    const body = await request.json();
    messages = body.messages as UIMessage[];
  } catch {
    return errorResponse('Invalid request body', 400);
  }

  let modelMessages;
  try {
    modelMessages = await convertToModelMessages(messages);
  } catch {
    return errorResponse('Failed to convert messages', 400);
  }

  // ── Stream ────────────────────────────────────────────────────────────────
  try {
    const google = createGoogleGenerativeAI({ apiKey: googleApiKey });
    const tools = user ? createMarsTools(ctx, user) : undefined;

    const result = streamText({
      model: google('gemini-2.0-flash'),
      system: buildSystemPrompt(user),
      messages: modelMessages,
      tools,
      maxSteps: 5,
    });

    const streamResponse = result.toUIMessageStreamResponse();
    const headers = new Headers(streamResponse.headers);
    for (const [key, value] of Object.entries(CORS_HEADERS)) {
      headers.set(key, value);
    }
    return new Response(streamResponse.body, {
      status: streamResponse.status,
      headers,
    });
  } catch {
    return errorResponse('AI service error', 500);
  }
}
```

- [ ] **Step 2: Update `convex/http.ts` to pass `ctx`**

Find the `/api/chat` POST route in `convex/http.ts`:

```typescript
// BEFORE
handler: httpAction(async (_ctx, request) => {
  return handleChatRequest(request);
}),
```

Change to:

```typescript
// AFTER
handler: httpAction(async (ctx, request) => {
  return handleChatRequest(ctx, request);
}),
```

- [ ] **Step 3: Verify TypeScript compiles**

```bash
npx convex dev --once 2>&1 | tail -8
```

Expected: `✔ Convex functions ready!`

If you see `getUserProfile` arg errors, run:
```bash
grep -A 8 "getUserProfile" convex/users/queries.ts
```
and adjust the arg name in `resolveUser` accordingly.

- [ ] **Step 4: Commit**

```bash
git add convex/livekit/chat.ts convex/http.ts
git commit -m "feat(ai): add JWT auth resolution and wire tools into streamText"
```

---

## Chunk 2: Frontend — Auth Header

### Task 5: Pass auth token in chat transport

**Depends on Task 4** (backend must be deployed before testing).

**Files:**
- Modify: `src/components/AiAssistantPanel.vue`

**Context:** `useUserStore()` exposes `token` as a reactive `Ref<string | null>`. `DefaultChatTransport` accepts `headers` as a function called on each request.

- [ ] **Step 1: Add `useUserStore` import to `AiAssistantPanel.vue`**

In the `<script setup>` block, after the existing imports, add:

```typescript
import { useUserStore } from '@/stores/userStore';
```

- [ ] **Step 2: Instantiate the store after `defineEmits`**

After the `defineEmits` line, add:

```typescript
const userStore = useUserStore();
```

- [ ] **Step 3: Update the `Chat` constructor (around line 283)**

Replace:

```typescript
const chat = new Chat({
  transport: new DefaultChatTransport({
    api: `${CONVEX_SITE_URL}/api/chat`,
  }),
});
```

With:

```typescript
const chat = new Chat({
  transport: new DefaultChatTransport({
    api: `${CONVEX_SITE_URL}/api/chat`,
    headers: () =>
      userStore.token ? { Authorization: `Bearer ${userStore.token}` } : {},
  }),
});
```

- [ ] **Step 4: Verify no TypeScript errors**

```bash
npx tsc --noEmit 2>&1 | grep "AiAssistantPanel" | head -10
```

Expected: no output.

- [ ] **Step 5: Commit**

```bash
git add src/components/AiAssistantPanel.vue
git commit -m "feat(ai): pass Authorization header from userStore in chat transport"
```

---

## Chunk 3: Deploy + Smoke Test

### Task 6: Deploy and verify end-to-end

- [ ] **Step 1: Deploy to Convex dev**

```bash
npx convex dev --once
```

Expected: `✔ Convex functions ready!`

- [ ] **Step 2: Start the frontend dev server**

```bash
npm run dev
```

- [ ] **Step 3: Smoke test — unauthenticated**

Without logging in, open the AI chat and send `"привет"`. Verify:
- Response comes back (general navigation help, no tool calls)
- No CORS errors in browser console

- [ ] **Step 4: Smoke test — authenticated teacher**

Log in as a teacher. Open AI chat. Ask: `"какие у меня журналы?"`

Expected: AI calls `listMyJournals` tool (visible in Network tab as streamed tool call chunks), then responds with actual journal names.

- [ ] **Step 5: Smoke test — multi-step chaining**

As teacher, ask: `"покажи оценки по первому журналу"`

Expected: AI calls `listMyJournals` → then calls `getJournalMarks` with the first result's `_id`.

- [ ] **Step 6: Smoke test — authenticated student (if account available)**

Log in as a student. Ask: `"какие у меня оценки?"`

Expected: AI calls `getMyMarks`, responds with grade data.

- [ ] **Step 7: Final commit**

```bash
git add -A
git commit -m "feat(ai): MARS MCP tools integration complete

- 13 role-scoped tools via createMarsTools
- JWT auth resolved in HTTP action
- Dynamic system prompt with user context and tool instructions
- Frontend passes auth token from userStore
- maxSteps: 5 for multi-step tool chaining"
```

---

## Known Limitations / Follow-ups

- **Student name matching is fragile:** Students have no `userId` field, so `resolveUser` finds them by `firstName + surname` match. Duplicate names will return the wrong student. Long-term fix: add `userId?: v.optional(v.id("users"))` to the `students` table and populate it during login.

- **Admin substitution listing:** No `listAll` query exists for substitutions. Admins without a `teacherId` see a message directing them to the Substitutions page. Add `api.substitutions.queries.listAll` as a follow-up.

- **Student schedule:** Loading all events and filtering by `participants` in memory works but is O(n) on total event count. Add a `getByParticipant` query with an index as a follow-up.
